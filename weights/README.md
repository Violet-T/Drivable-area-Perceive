# Weights

Place model checkpoints here instead of keeping them in the repository root.

Recommended layout:

```text
weights/
├── YOLOP/
├── SEA-RAFT/
└── STGRU/
```

Large checkpoint files should not be committed to Git.

